#include "Server.h"
#include "ProxyJugador.h"
/*Borralos de aca abajo tambien*/

Server::Server():jugador(mundo.getMegaman(),*ventana,*serverProxy){}

void Server::inicializar(){
	Simulador(mundo,100);
}

ProxyJugador& Server::getJugador(){
	return jugador;
}
